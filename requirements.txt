# --- Core ---
streamlit==1.37.0
python-dotenv==1.0.1

# --- Playwright (sync API) ---
playwright==1.48.0

# --- Utils ---
pandas==2.2.2
requests==2.32.3

# --- (Optional) for Bright Data / Proxy auth helpers ---
websockets==12.0

# --- Needed to install browser binaries (run once after pip install) ---
# playwright install chromium
